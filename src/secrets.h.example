#pragma once
// Template for secrets.h. Copy this file to secrets.h and fill in real values.
//   cp src/secrets.h.example src/secrets.h
// secrets.h is gitignored so your credentials never get committed.

// WiFi AP (device hotspot) credentials — choose your own
#define AP_SSID "CHANGE_ME"
#define AP_PASS "CHANGE_ME_min_8_chars"
